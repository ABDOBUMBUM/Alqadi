# 🚀 استراتيجية رفع المشروع على GitHub (GitHub Upload Strategy)

> **المشكلة:** فشل الرفع بسبب "الملفات الثقيلة" (LFS Objects) ومشاكل في DNS للاتصال بسيرفرات Amazon S3 الخاصة بـ GitHub.
> **الحل:** فلترة الملفات، تحديث التجهيزات، والرفع بدون الملفات التي تسبب المشكلة حالياً.

---

## 📂 أولاً: تصنيف الملفات (Important vs. Unimportant)

### 1. ملفات مهمة جداً (يجب رفعها) ✅
- **`src/`**: الكود البرمجي بالكامل (الصفحات، المكونات، APIs).
- **`prisma/`**: هيكل قاعدة البيانات (`schema.prisma`).
- **`package.json` & `package-lock.json`**: قائمة المكتبات المطلوبة.
- **`public/brand/`**: الشعارات الأساسية (Icons, Logos).
- **الملفات النصية**: (`*.json`, `*.ts`, `*.tsx`, `*.md`).
- **التوثيق**: (`FULL_STACK_GUIDE.md`, `SECURITY_AUDIT.md`, `DESIGN.md`).

### 2. ملفات "ثقيلة" أو غير مهمة (يجب تجاهلها) ❌
- **`.next/`**: (3.6 GB!) هذا مجلد مؤقت للبناء ولا يجب رفعه أبداً.
- **`node_modules/`**: (1.2 GB!) يتم تحميله آلياً عبر `npm install`.
- **`.env`**: (Security!) يحتوي على كلمات المرور ولا يجب رفعه.
- **`extracted_*.txt`**: ملفات نصوص مستخرجة (مؤقتة).
- **`full_page.png`**: صورة شاشة كاملة كبيرة الحجم.
- **`public/assets/`**: (LFS) ملفات الـ 3D والصور عالية الدقة (إذا كانت تسبب فشل الرفع).

---

## 🛠️ ثانياً: خطوات التنظيف والرفع (الخطة العملية)

قم بتنفيذ هذه الأوامر في Terminal داخل المجلد `a:\Alqadi\web`:

### 1. تحديث قائمة التجاهل (.gitignore)
لقد قمت بتحديث ملف `.gitignore` لك ليتجاهل تلقائياً كل ما هو غير مهم (بما في ذلك `.next` و `node_modules` و `.env`).

### 2. إزالة الملفات "الثقيلة" من ذاكرة Git (Index)
بما أنك حاولت الرفع سابقاً، قد تكون بعض الملفات الثقيلة "علقة" في ذاكرة Git. قم بتنظيفها:
```powershell
# إزالة كل الملفات من الذاكرة المؤقتة (لن يحذف الملفات من جهازك)
git rm -r --cached .

# إعادة إضافة الملفات بناءً على قواعد .gitignore الجديدة
git add .
```

### 3. حل مشكلة LFS (اختياري ولكن موصى به حالياً)
إذا كنت لا تستطيع الوصول لسيرفرات LFS، يفضل تعطيله مؤقتاً لرفع الكود البرمجي أولاً:
```powershell
# إزالة إعدادات LFS من الملفات
git lfs uninstall
# (اختياري) إذا أردت رفع الصور كملفات عادية بدلاً من LFS
# قم بحذف ملف .gitattributes أو تعديله
```

### 4. الرفع النهائي
```powershell
git commit -m "chore: cleanup project and filter important files for upload"
git push -u origin main
```

---

## 💡 نصائح إضافية:
- **الملفات الكبيرة جداً**: إذا كان هناك ملف `.glb` أو فيديو في `public/assets` يتجاوز 50MB، فسيمنعك GitHub من رفعه بدون LFS. يفضل نقله خارج المجلد مؤقتاً حتى ينجح الرفع.
- **DNS**: إذا استمرت مشكلة `lookup github.com: no such host` كرر عملية `ipconfig /flushdns` أو استخدم DNS جوجل (`8.8.8.8`).

---

> **النتيجة بعد هذه الخطوات:** سينخفض حجم الرفع من جيجابايت إلى **أقل من 5 ميجابايت** (للكود فقط)، وسينجح الرفع في ثوانٍ معدودة!
