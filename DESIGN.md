# ⚜️ مجموعة القاضي الذهبية - Design System & Architecture

يُوثق هذا الملف (`DESIGN.md`) نظام التصميم الشامل والهوية البصرية التقنية المعتمدة في منصة **مجموعة القاضي الذهبية** (Al-Qadi Golden Group). تم بناء الواجهات لتواكب أحدث معايير الرفاهية الرقمية، مع التركيز على الأداء العالي، الديناميكية، والتجاوب الكامل مع كافة الأجهزة.

---

## 1. الفلسفة التصميمية (Design Philosophy)

ترتكز الهوية البصرية للمنصة على الفخامة المؤسسية الحديثة (Modern Corporate Luxury).
- **الاحترافية والموثوقية:** انعكاس لتاريخ الشركة (45 عاماً) في تقديم خدمات السفر والأيادي العاملة.
- **التصميم الموجه للمستخدم (UX):** التدرج البصري الواضح، التباين العالي للقراءة، والمساحات السلبية المريحة (Negative Space).
- **اللمسة الملكية:** استخدام درجات الذهب النبيل مع تأثيرات الرخام والزجاج (Glassmorphism) لتعزيز الإحساس بالفخامة.

---

## 2. الألوان والسمات (Color Palette & Theming)

يستخدم الموقع نظام Theme ديناميكي مدمج يعتمد على متغيرات CSS (CSS Variables) مخصصة لتوفير نسختين متكاملتين: الوضع الداكن والوضع الفاتح.

### 🌓 الوضع الداكن (Dark Mode - Default)
يُعتبر الوضع الأساسي للموقع، حيث يوفر تبايناً مذهلاً للألوان الذهبية.
- **الخلفيات (Backgrounds):**
  - `--bg-void`: `#000000` (أسود خالص للأعماق)
  - `--bg-deep` / `--page-bg`: `#050404` (خلفية الصفحات الأساسية)
  - `--bg-card` / `--page-surface`: `rgba(255,255,255,0.02)` (سطح البطاقات بلمسة شفافة)
- **النصوص (Typography):**
  - `--page-text`: `#f5f1e8` (أبيض كريمي لتقليل إجهاد العين)
  - `--page-text-muted`: `rgba(245,241,232,0.55)` (للنصوص الثانوية)

### 🌔 الوضع الفاتح (Light Mode)
يوفر بديلاً مشرقاً واحترافياً يعتمد على درجات الأبيض والرخام الفاتح للحصول على طابع نقي (Clean & Crisp).
- **الخلفيات (Backgrounds):**
  - `--page-bg`: `#fcfcfc` (أبيض نقي مكسور)
  - `--page-surface`: `#ffffff` (أبيض ساطع للبطاقات مع ظل خفيف)
  - `--page-border-subtle`: `rgba(0,0,0,0.06)` (حدود خفيفة جداً للتباين)
- **النصوص (Typography):**
  - `--page-text`: `#1a1a1a` (أسود داكن للقراءة الواضحة)
  - `--page-text-muted`: `rgba(0,0,0,0.6)`

### 🌟 لون الهوية (The Gold Accent)
اللون الذهبي هو البصمة الأساسية، وتم تعريف تدرجاته في إعدادات Tailwind لتوفير تأثيرات مضيئة (Glow) وانتقالات (Gradients) ناعمة:
- **تدرجات الذهبي:** `gold-300`, `gold-400` (للأيقونات والنصوص), `gold-500` (للأزرار والتأثيرات النشطة), `gold-600`.
- التدرج الخطي المعتمد للنصوص البارزة: `.text-gold-gradient` (يمنح بريقاً معدنياً).

---

## 3. التأثيرات والملمس (Textures & Effects)

### تأثير الرخام (Marble Background)
تم إضافة تأثير بصري معقد للصفحات يجمع بين الحبيبات (Grain Noise) وشبكات التدرج:
- `.marble-bg`: يُضيف طبقة عزل جمالية تشبه عروق الرخام (عبر `radial-gradient` وحبيبات ضوضائية بنسبة `opacity: 0.4` للداكن و `0.1` للفاتح).

### تأثير الزجاج (Glassmorphism)
يُستخدم في البطاقات، النوافذ المنبثقة، وقوائم التنقل (Navbars).
- يتم تحقيقه عبر دمج `backdrop-blur` مع خلفيات شبه شفافة `rgba(255,255,255,0.02)`.
- تم تجسيد ذلك في مكونات أساسية مثل `<GlassPanel hoverGlow={true} />`.

---

## 4. المكونات الأساسية (Core UI Components)

- **البطاقات (Cards/Panels):**
  - زوايا مستديرة ناعمة `rounded-2xl` أو `rounded-3xl`.
  - حدود رقيقة جداً `--page-border-subtle` تتفاعل عند التمرير (Hover) لإنشاء تأثير إضاءة `hover:border-gold-500/20`.
- **الأزرار (Buttons):**
  - **الأزرار الأساسية (Primary):** خلفية ذهبية صلبة `bg-gold-500` مع نص داكن، يتحول إلى `bg-gold-400` عند التمرير.
  - **الأزرار المتدرجة:** `.btn-gold`، تدرج لوني يعطي إيحاء المعدن.
  - **الأزرار الشفافة (Ghost):** حدود ذهبية رقيقة وخلفية شفافة تتوهج عند التمرير.
- **حقول الإدخال (Inputs):**
  - خلفيات داكنة/فاتحة حسب الـ Theme.
  - تتألق الحدود بخط ذهبي `focus:border-gold-500/50` عند التركيز (Focus).

---

## 5. الحركة والأنيميشن (Motion & Animation)

تم دمج مكتبة **Framer Motion** لإنشاء تجربة تفاعلية حية بدون التأثير على الأداء:
- **الدخول التدريجي (Reveal):** النصوص والبطاقات تدخل الشاشة بحركة عمودية ناعمة مع ظهور تدريجي `opacity: 0, y: 28` إلى `opacity: 1, y: 0`.
- **Micro-interactions:** 
  - الأزرار تصغر وتكبر بخفة عند الضغط (Scale).
  - الأيقونات تتفاعل بلون ذهبي نابض عند التركيز.
- **التمرير السلس:** انتقال سلس بين الأقسام.

---

## 6. الأيقونات والرسومات (Icons & Imagery)

- **الأيقونات:** الاعتماد بالكامل على مكتبة `lucide-react` لضمان اتساق سماكة الخطوط (Stroke Width) والمظهر البسيط (Minimalist).
- **الصور (Imagery):** صور عالية الدقة من (Unsplash/مخصصة)، يتم معالجتها لتناسب الوضع الداكن (فلاتر تعتيم طفيفة `brightness/contrast`) لتجنب إبهار العين وضمان انسجامها مع الواجهة.

---

## 7. الهيكلية الإدارية (Admin UI Architecture)

لوحة تحكم الإدارة (Admin Panel) تحتفظ بنفس لغة التصميم الفخمة لكن مع التركيز على **الكثافة المعلوماتية (Information Density) والتطبيق العملي**:
- شريط جانبي (Sidebar) داكن ورفيع يفصل أقسام الـ ERP.
- قوائم بيانية للتحكم السريع في (حجوزات، موظفين، تأشيرات، فنادق، وأيادي عاملة).
- **CMS Editor:** محرر JSON مع تنسيق لوني لسهولة تحديث المحتوى الديناميكي دون المساس بالواجهة الأمامية.

---

## 8. الأداء والتجاوب (Performance & Accessibility)

- **Mobile First:** التصميم مبني ليقدم تجربة أصلية (Native-like) على الهواتف المحمولة أولاً، مع توسع ذكي للشبكات (Grids) على الشاشات الكبيرة (Tailwind breakpoints: `sm`, `md`, `lg`, `xl`).
- **Contrast Ratios:** تضمن متغيرات الألوان تباينًا يتوافق مع معايير الوصول (WCAG AA).
- **عزل المكونات (Component Isolation):** الواجهة مقسمة إلى Client Components (للتفاعل) و Server Components (لجلب البيانات وتحسين الـ SEO وسرعة التحميل).

---
> 🌟 *صُمم هذا النظام البصري ليعكس الهيبة، الثقة، والمرونة المطلقة التي تقدمها مجموعة القاضي لعملائها في كل رحلة وكل معاملة.*
